
import { createMuiTheme } from '@material-ui/core/styles';

const palette = {
  primary: { main: '#f5f2f2', contrastText: '#2D4454' },
  secondary: { main: '#BC8905', contrastText: '#ffffff' }
};
const themeName = 'Natural Gray Pirate Gold Ostrich';

export default createMuiTheme({ palette, themeName });