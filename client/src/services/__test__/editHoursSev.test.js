import {filterData,getMonthName} from "../editHoursSev"

describe('test the helper functions',()=>{
    test('should return array that passes the condition',async ()=>{
      const filterData= jest.fn()
      const object = [1,2,3,4,5,6]
      
    })
})


